package Introduction.Methods;

public class Step01_Method {
    public static void main(String[] args) {

        // Bloco de código nomeado que realiza uma tarefa específica e pode retornar um valor
        // Pode receber parâmetros, e ser invocado quantas vez for necessário

        // Parâmetros - Variáveis declaras na definição do métoodo
        // Argumentos - Valores passados para o métoodo na chamada

        /* Anatomia
        * - Modificadores:
        *       - De acesso(public, private, protected, ou default) definem visibilidade
        *       - De comportamento: static - pertence a classe
        *                           final - não pode ser modificado depois de atribuído
        *                           abstract - não tem implementação (usado em classes abstratas)
        *                           synchronized - acesso apenas por uma Thread de cada vez
        * - Tipo de retorno (void se não houver)
        * - Nome deve seguir convenção de nomenclatura (camelCase)
        * - Parâmetros(opcional) - Lista de valores recebido
        * - Corpo - {} Bloco de código que contém a lógica executada
        *
        * */


    }
}
